# Dependencies used only by the dataframe media-upload integration execution.
# The product client itself is installed from the case-declared immutable wheel.
-r requirements.txt
google-cloud-bigquery==3.43.0
pandas-gbq==0.35.1
pandas>=1.5.3
pyarrow>=12.0.0
