[pytest]
addopts = -ra --strict-markers
timeout = 90
timeout_method = thread
markers =
    gap(id): Explicit unsupported-capability contract with a stable gap ID.
    operation(id): Public operation ID from contract/operations.normalized.json.
