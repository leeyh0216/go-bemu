[pytest]
addopts = --strict-config --strict-markers --timeout=600 --timeout-method=thread --tb=short -p no:cacheprovider -ra
testpaths = tests/integration/spark
markers =
    contract_case(id, **metadata): literal capability claim generated into the integration index
