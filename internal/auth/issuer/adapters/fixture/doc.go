// Package fixture verifies explicitly registered local credentials and RSA
// service-account keys.
//
// These adapters are deterministic emulator fixtures. They are not Google IAM,
// workload identity provider, OAuth client registry, or key-discovery parity.
package fixture
